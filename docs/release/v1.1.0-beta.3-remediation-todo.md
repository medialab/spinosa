# v1.1.0-beta.3 release remediation TODO

**Audit verdict: DO NOT RELEASE.** Local remediation is complete and validated; the release still needs a clean committed release tree and publication.

## Release identity

- Version: `1.1.0-beta.3`
- Tag/channel: `v1.1.0-beta.3` / rolling `beta`
- Audited commit: `1967dd038afd427caf48029d652c762e9d6d6050`
- Release date: `2026-09-02`
- Current branch: `codex/beta-dev-swarm-integration`
- Audited assets: four binaries, `install.sh`, `checksums.txt`, `build-manifest.json`, GitHub source archives

## P0 — release blockers

- [ ] **Republish installers from a clean release commit.** The local `install.sh` fix now persists `SPINOSA_HOME` through fresh shells, including spaces and Unicode paths. Carry it into the release commit and republish both immutable `v1.1.0-beta.3` and rolling `beta` installers.

- [x] **Regenerate the template pack from tracked inputs.** The pack ID is now `eb9883d33e28df5557ace27e8aaad040e1454df9aefef54b6d4b30a2e2ac3def`; generated-file and template-input checks pass.

- [x] **Remove personal machine paths from the source archive.** The four audited source files are now free of contributor home, repository, prompt-plan, and local database paths.

- [x] **Rebuild and scan all four targets locally.** Darwin arm64/x64 and Linux arm64/x64 binaries rebuild successfully, contain no `/Users/`, `/home/`, or `C:\Users\` prefixes, and pass the strict host smoke path. The jsdom build-machine `require.resolve()` path is removed before packaging.

- [ ] **Cut from a clean, committed release tree.** Commit the portability fixes and generated resources, remove unintended untracked files, confirm `git status` is clean, then cut the replacement tag and rolling channel. Do not publish the current dirty worktree.

## P1 — current-branch quality

- [x] **Fix the release-critical TUI workspace-update contract.** Missing registered workspaces now return `{ success: true, skipped: 1 }`; focused and full quality tests pass.

- [x] **Make the full quality gate green.** `bun run quality` passes both waves, including the TUI suite.

## P2 — hygiene regression protection

- [x] Keep installer regression coverage in `tests/installer/binary.bats`, including custom `SPINOSA_HOME`, fresh shells, spaces, and Unicode paths.

- [x] Keep tracked-only template-input coverage in `scripts/release/template-inputs.ts` and `scripts/release/template-inputs.test.ts`.

- [x] Add binary path scans to the release build. The build fails on developer home prefixes and scrubs build paths before Darwin re-signing; all four rebuilt binaries pass the forbidden-prefix scan.

- [x] Investigate third-party native metadata paths. The build applies a narrow scrub to the exact Rust Cargo registry prefix emitted by the cross-build image; arbitrary `C:\\Users` paths remain untouched to avoid binary corruption.

## Final replacement-release gate

- [ ] Rebuild and checksum all four binaries, `install.sh`, `checksums.txt`, `build-manifest.json`, and the template pack in the clean release commit. Local artifacts are rebuilt and checksummed; commit/tag publication remains pending.

- [x] Run `bun run quality`, `SPINOSA_BINARY_SMOKE_STRICT=1 bun run quality:binary`, strict version/doctor smoke tests, installer smoke tests, cross-target builds, embedded-resource checks, and generated-file checks.

- [x] Verify immutable release checksums and rolling `beta` installer checksums match locally generated files.

- [ ] Verify the GitHub source archive matches the replacement release tag and contains zero personal machine paths.

- [ ] Repeat generic source, generated-file, template, installer, and binary scans against the exact assets published by GitHub.

- [ ] Record final release identity, scan output, checksums, and any approved harmless metadata before changing the verdict to `SAFE TO RELEASE`.

## Fixes already present in the worktree

These edits exist locally and must be carried into the clean release commit and rebuilt artifacts:

- [x] Installer custom-home persistence and shell-path quoting in `install.sh`.
- [x] Tracked-only template packing in `scripts/pack-workspace-template.ts` and `scripts/release/template-inputs.ts`.
- [x] Source path cleanup in the four files listed under the P0 source-archive blocker.
- [x] Compile-safe module detection and PDF worker handling in core and kernel runtime paths.
- [x] Compiled-binary `doctor` probes avoid eagerly loading heavy OCR/canvas natives during installer verification.
