Classify a Spinosa workspace request into general vs orchestrated intent dimensions using deterministic routing rules.

Use before framing any multi-step research work. Returns a RouteDecision JSON plus provenance. When the result is provisional, the rules abstained: apply your own judgment and override the fallback when the request clearly needs more (or less) than it claims.
