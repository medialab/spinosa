Frame a Spinosa research run: mint a run ID, resolve the workflow plan, and write the goal artifact that scopes all later work.

Use after routing decides orchestrated and before dispatching any research subagent. General (direct-answer) decisions are never framed. Returns runID, workflowID, and goal path for downstream steps.
