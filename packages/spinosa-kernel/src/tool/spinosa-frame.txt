Start a research run: write a goal file, pick a plan, and hand back a run id.

Call this after spinosa_route says the work is orchestrated. Pass that same decision through — especially the short `strategy` name. Do not invent a pipeline sentence. Direct chat answers do not need this tool.

Returns a run id, a plain-language plan name, and the goal file path. Later tools use the run id, not the plan id.
